#include "Color.h"

namespace Common
{
    Color::Color(const uint8_t r, const uint8_t g, const uint8_t b, const uint8_t a)
    {
        this->r = r;
        this->g = g;
        this->b = b;
        this->a = a;
    }
}
